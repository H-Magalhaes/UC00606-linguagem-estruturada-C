/******************************************************************************
3. Elabore um programa em C, que armazene 10 números num vetor inseridos 
pelo utilizador. Conte e escreva quantos valores pares existem no vetor. 
*******************************************************************************/
#include <stdio.h>
#include <locale.h>

#define T_Max 10


int main()
{
    setlocale(LC_ALL, "Portuguese");

    
    int soma=0, i, j, cont=0;
    int v_num[T_Max];
    
    for(i=0; i<T_Max; i++){
      printf("Insira %dº número: ", i+1);
      scanf("%d", &v_num[i]);
  }
    for(i=0; i<T_Max; i++){
        if(v_num[i]%2==0){
      printf("Os numeros pares são: %d, na posicao %d\n", v_num[i], i+1);
     
      cont++;
  }
    }
     printf("No vetor existem %d números par", cont);
    
}
